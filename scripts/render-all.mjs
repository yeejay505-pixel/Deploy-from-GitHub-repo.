import path from 'node:path';import fs from 'node:fs/promises';import {fileURLToPath} from 'node:url';import {bundle} from '@remotion/bundler';import {renderMedia,selectComposition} from '@remotion/renderer';import manifest from '../src/manifest.json' with {type:'json'};const here=path.dirname(fileURLToPath(import.meta.url)),root=path.resolve(here,'..'),out=path.join(root,'outputs');await fs.mkdir(out,{recursive:true});const serveUrl=await bundle({entryPoint:path.join(root,'src/index.jsx')});for(const scene of manifest.scenes){const inputProps={scene},composition=await selectComposition({serveUrl,id:'Scene',inputProps});await renderMedia({composition,serveUrl,codec:'h264',outputLocation:path.join(out,`${manifest.project_id}_${scene.scene_id}.mp4`),inputProps,crf:18,pixelFormat:'yuv420p',muted:true});console.log('Rendered',scene.scene_id);}
